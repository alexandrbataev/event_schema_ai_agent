{
  "description": "Закрыта модалка пользователем (не довели верификацию или аутентификацию)",
  "properties": {
    "screen": {
      "description": "на каком экране модалки произошло событие: на вводе телефона или на вводе смс кода",
      "type": "string",
      "enum": [
        "enter_phone",
        "enter_sms_code"
      ]
    },
    "is_signed_in": {
      "description": "залогинен ли пользователь",
      "type": "boolean"
    },
    "scenario": {
      "description": "сценарий, для которого был ввод телефона: verification | edit | auth",
      "type": "string",
      "enum": [
        "verification",
        "edit",
        "auth"
      ]
    }
  },
  "required": [
    "screen",
    "is_signed_in",
    "scenario"
  ],
  "title": "Phone Modal Dismissed",
  "type": "object"
}