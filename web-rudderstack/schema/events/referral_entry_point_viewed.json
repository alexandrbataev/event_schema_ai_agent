{
  "description": "Каждый раз, когда пользователь видит точку входа в раздел 'Пригласить друга', по нажатию на которую происходит переход на страницу реферальной программы",
  "properties": {
    "source": {
      "description": "Где была отрисована точка входа",
      "type": "string",
      "enum": [
        "profile",
        "your_promocodes"
      ]
    },
    "type": {
      "description": "Тип элемента",
      "type": "string",
      "enum": [
        "banner",
        "promocode",
        "button"
      ]
    }
  },
  "required": [
    "source",
    "type"
  ],
  "title": "Referral Entry Point Viewed",
  "type": "object"
}