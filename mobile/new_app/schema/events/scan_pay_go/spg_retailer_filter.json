{
  "description": "Выбран ритейлер для самовывоза",
  "properties": {
    "available_retailers": {
      "description": "Массив с характеристиками доступных ритейлеров: position (позиция магазина на странице, нумерация с 1), store_id - id магазина, который уже был выбран для самовывоза, если такого магазина нет, то ничего не отправляем",
      "type": "array",
      "items": {
        "$ref": "#/definitions/PickupRetailer"
      }
    },
    "retailer_clicked": {
      "description": "массив с характеристиками выбранного ритейлера store_id - id магазина, который уже был выбран для самовывоза, возможно только при source = with_pickup_store нумерация position с 1",
      "type": "array",
      "items": {
        "$ref": "#/definitions/PickupRetailer"
      }
    },
    "action": {
      "type": "string",
      "description": "С какого таба был выбран ритейлер (фильтры 2го уровня). Фильтры 2го уровня это косметика, алкоголь, аптеки и т.п.",
      "enum": [
        "apply",
        "remove"
      ]
    }
  },
  "required": [
    "available_retailers",
    "retailer_clicked",
    "action"
  ],
  "title": "Spg Retailer Filter",
  "type": "object"
}
