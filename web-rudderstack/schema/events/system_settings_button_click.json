{
  "description": "Нажатие на кнопку с разрешением системного доступа",
  "properties": {
    "setting": {
      "description": "Какой системный параметр настраивается?",
      "type": "string",
      "enum": [
        "notifications_access"
      ]
    },
    "access": {
      "description": "Дано ли разрешение на доступ",
      "type": "string",
      "enum": [
        "denied",
        "allowed"
      ]
    }
  },
  "required": [
    "access",
    "setting"
  ],
  "title": "System Settings Button Click",
  "type": "object"
}