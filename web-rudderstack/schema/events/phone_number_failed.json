{
  "description": "Появилась ошибка под полем ввода номера телефона",
  "properties": {
    "is_signed_in": {
      "description": "залогинен ли пользователь",
      "type": "boolean"
    },
    "scenario": {
      "description": "сценарий, для которого был ввод телефона: verification | edit | auth",
      "type": "string",
      "enum": [
        "verification",
        "edit",
        "auth"
      ]
    },
    "fail_reason": {
      "description": "Причина ошибки",
      "type": "string"
    }
  },
  "required": [
    "scenario",
    "is_signed_in",
    "fail_reason"
  ],
  "title": "Phone Number Failed",
  "type": "object"
}