{
  "description": "На экране пользователя отрисовался блок с опциями мультикарточки",
  "properties": {
    "product_sku": {
      "description": "SKU карточки, где отрисовался блок с опциями",
      "type": "string"
    },
    "available_options": {
      "description": "Список атрибутов для переключения мультикарточки, их значений и состояний",
      "type": "array",
      "items": {
        "$ref": "#/definitions/MulticardOptions"
      }
    }
  },
  "required": [
    "product_sku",
    "available_options"
  ],
  "title": "Multicard Block Viewed",
  "type": "object"
}