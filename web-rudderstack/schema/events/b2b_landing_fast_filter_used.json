{
  "description": "Срабатывает каждый раз когда пользователь кликает на плашки с фильтрами на главной",
  "properties": {
    "landing_type": {
      "description": "Тип лендинга, на котором сработало событие",
      "type": "string",
      "enum": [
        "office",
        "traders",
        "horeca"
      ]
    },
    "filter_name": {
      "description": "Название фильтра",
      "type": "string"
    }
  },
  "required": [
    "filter_name"
  ],
  "title": "B2B Landing Fast Filter Used",
  "type": "object"
}
