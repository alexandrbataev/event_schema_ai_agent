{
  "description": "Каждый раз при использовании A/B теста, в том месте где код смотрит на значение параметров для конкретного label",
  "properties": {
    "feature_state": {
      "type": "string",
      "description": "Значения feature_state из ответа ручки split_customer, для конкретного label,используемого в A/B"
    },
    "label": {
      "type": "string",
      "description": "Тот лейбл, на который завязываемся в конкретном A/B"
    }
  },
  "required": [
    "label",
    "feature_state"
  ],
  "title": "AB Test Used on Client",
  "type": "object"
}
