{
  "description": "Каждый раз, при выбор адреса из списка адресов в модалке со списком адресов",
  "properties": {
    "source": {
      "description": "Откуда перешли в список адресов",
      "$ref": "#/definitions/AddressChangeSource"
    },
    "address_count": {
      "description": "Количество сохраненных адресов",
      "type": "number"
    },
    "address_full_title": {
      "description": "Полный адрес",
      "type": "string"
    },
    "address_subtitle": {
      "description": "Subtitle адреса (то, что указано под основным адресом маленькими буквами - как правило страна, город)",
      "type": "string"
    }
  },
  "required": [
    "source",
    "address_count",
    "address_full_title",
    "address_subtitle"
  ],
  "title": "Address Selected From List",
  "type": "object"
}
