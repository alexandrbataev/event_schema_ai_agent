{
  "description": "Нажата кнопка получения кода по СМС (или клавиша Enter)",
  "properties": {
    "is_signed_in": {
      "description": "залогинен ли пользователь",
      "type": "boolean"
    },
    "scenario": {
      "description": "сценарий, для которого был ввод телефона: verification | edit | auth",
      "type": "string",
      "enum": [
        "verification",
        "edit",
        "auth"
      ]
    }
  },
  "required": [
    "scenario",
    "is_signed_in"
  ],
  "title": "Phone Number Started",
  "type": "object"
}
