{
  "description": "показ попапа с текстом, что RX можно купить только в магазине",
  "properties": {
    "source": {
      "type": "string",
      "description": "Источник экрана, с кот был вызван попап.",
      "enum": [
        "category",
        "product",
        "search",
        "favourite",
        "search_multiretail",
        "cart"
      ]
    },
    "type": {
      "type": "string",
      "description": "Используется для определения типа попапа. Например, в алкоголе можно ввести такой же с type = alcohol.",
      "enum": [
        "prescription"
      ]
    }
  },
  "required": [
    "source",
    "type"
  ],
  "title": "Pickup Popup Viewed",
  "type": "object"
}
