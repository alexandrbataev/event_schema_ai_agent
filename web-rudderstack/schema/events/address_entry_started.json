{
  "description": "Событие срабатывает на карте, при использовании строки поиска, после того как пользователь начал писать что-то в строку. Для отправки достаточно одного любого символа.",
  "properties": {
    "source": {
      "$ref": "#/definitions/AddressChangeSource"
    },
    "map_type": {
      "description": "На какой карте был открыт поиск: доставка/самовывоз",
      "type": "string",
      "enum": [
        "pickup_map",
        "delivery_map"
      ]
    }
  },
  "required": [
    "source",
    "map_type"
  ],
  "title": "Address Entry Started",
  "type": "object"
}