{
  "description": "Каждый раз, когда пользователь кликает на Выбрать предзамену в блоке состава заказа на чекауте и постчекауте",
  "properties": {
    "is_oos_risky": {
      "description": "1, если оригинальный товар имеет пометку Может закончиться, 0 иначе",
      "type": "boolean"
    },
    "product_sku_orig": {
      "description": "Скю оригинального товара, для которого выбирается предзамена, если он закончится",
      "type": "string"
    },
    "source": {
      "description": "Откуда выбиралась предзамена - из чекаута или постчекаута",
      "type": "string",
      "enum": [
        "checkout",
        "postcheckout"
      ]
    },
    "order_number": {
      "description": "order_number для которого выбирается предзамена",
      "type": "string"
    }
  },
  "required": [
    "is_oos_risky",
    "product_sku_orig",
    "source",
    "order_number"
  ],
  "title": "Prereplacement Selection Started",
  "type": "object"
}
