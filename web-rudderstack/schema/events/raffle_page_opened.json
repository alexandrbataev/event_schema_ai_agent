{
  "description": "Отпрвляется при каждой загрузке страницы розыгрыша",
  "properties": {
    "source": {
      "description": "Страница, с которой пользователь перешел на экран розыгрыша",
      "$ref": "#/definitions/RaffleSource"
    },
    "utm_source": {
      "description": "utm параметр из внешней ссылки",
      "type": "string"
    },
    "utm_medium": {
      "description": "utm параметр из внешней ссылки",
      "type": "string"
    },
    "utm_campaign": {
      "description": "utm параметр из внешней ссылки",
      "type": "string"
    },
    "campaign_name": {
      "description": "Название кампании",
      "type": "string"
    },
    "is_participant": {
      "description": "Пользователь участвует в розыгрыше (True) или видит приветственный экран (False)",
      "type": "boolean"
    }
  },
  "required": [
    "source",
    "campaign_name",
    "is_participant"
  ],
  "title": "Raffle Page Opened",
  "type": "object"
}
