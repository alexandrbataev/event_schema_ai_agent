{
  "description": "Каждый раз, когда пользователь нажимает на какой-либо из фильтров",
  "properties": {
    "source": {
      "description": "Где именно случилось событие: при просмотре списка товаров в каталоге или результатов поиска",
      "type": "string",
      "enum": [
        "search",
        "category"
      ]
    },
    "filter_name": {
      "description": "Название фильтра, на который нажали",
      "type": "string",
      "examples": [
        "Бренд",
        "Тип продукта"
      ]
    },
    "source_identifier": {
      "description": "Идентификатор открытия модалки с фильтрами в соответствии с источником - поисковый запрос, category_id",
      "type": "string",
      "examples": [
        "молоко",
        "4621"
      ]
    },
    "source_type": {
      "description": "Источник применения фильтра: через топ фильтры вверху выдачи или через кнопку всех фильтров. При клике на кнопку «Все фильтры» срабатывает всегда. В случае клика по топ фильтрам срабатывает только в случае, если фильтр имеет значения, которые нужно прокликать в отдельной открытой модалке. filter_chips - клик по топ фильтру вверху выдачи категории, filters_modal - клик по кнопке «Все фильтры»",
      "type": "string",
      "enum": [
        "filter_chips",
        "filters_modal"
      ]
    },
    "filters": {
      "description": "Массив, с выбранными до этого, фильтрами",
      "type": "array",
      "items": {
        "$ref": "#/definitions/Filters"
      }
    }
  },
  "required": [
    "source",
    "filter_name",
    "source_identifier"
  ],
  "title": "Product Filter Initiated",
  "type": "object"
}
