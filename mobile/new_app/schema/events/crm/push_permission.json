{
  "description": "Актуальный статус разрешения получения пушей",
  "properties": {
    "push_allowed": {
      "description": "Параметр показывает, разрешил ли получение пушей пользователь.",
      "type": "boolean"
    },
    "permission_changed": {
      "description": "Параметр показывает, изменился ли push_allowed в сравнении с предыдущим значением",
      "type": "boolean"
    }
  },
  "required": [
    "push_allowed",
    "permission_changed"
  ],
  "title": "Push Permission",
  "type": "object"
}
