{
  "description": "Срабатывает, когда пользователь закрывает баннер",
  "properties": {
    "source": {
      "description": "Где именно был увиден баннер",
      "type": "string",
      "enum": [
        "",
        "category",
        "banner_category_main",
        "million_spasibo_page"
      ]
    },
    "native_ad_id": {
      "description": "Уникальный идентификатор баннера",
      "type": "string"
    },
    "url": {
      "description": "Ссылка на страницу, на которую ведет баннер при клике",
      "type": "string"
    },
    "how_closed": {
      "description": "Как был закрыт баннер",
      "type": "string",
      "enum": [
        "close_button",
        "click_on_free_screen"
      ]
    }
  },
  "required": [
    "source",
    "native_ad_id"
  ],
  "title": "Banner Closed",
  "type": "object"
}