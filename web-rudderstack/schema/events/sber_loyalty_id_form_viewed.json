{
  "description": "Каждый раз при отображении модалки 'Войти по СберID'",
  "properties": {
    "source": {
      "description": "После нажатия на какой элемент пользователь попал на модалку",
      "type": "string",
      "enum": [
        "button",
        "banner",
        "cart",
        "order_completed_spasibo"
      ]
    },
    "is_welcome_offer": {
      "description": "Если пользователю на момент открытия модалки доступен велкам оффер",
      "type": "boolean"
    }
  },
  "required": [
    "source"
  ],
  "title": "Sber Loyalty Id Form Viewed",
  "type": "object"
}
