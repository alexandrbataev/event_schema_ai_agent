{
  "description": "Каждый раз, когда мердж временной корзины в основную заканчивается ошибкой (мердж не происходит)",
  "properties": {
    "retailer_id": {
      "description": "Идентификатор ритейлера во временной корзине",
      "type": "number"
    },
    "product_skus": {
      "description": "Массив словарей товаров во временной корзине (sku и quantity по каждому товару)",
      "type": "array",
      "items": {
        "$ref": "#/definitions/ShareCartProduct"
      }
    },
    "cart_id": {
      "description": "Идентификатор временной корзины",
      "type": "string"
    },
    "source": {
      "description": "Источник добавления (мерджа) временной корзины в основную",
      "type": "string"
    },
    "merge_mistake": {
      "description": "Если известна какая ошибка - записываем, если нет - пустое поле",
      "type": "string"
    }
  },
  "required": [
    "retailer_id",
    "product_skus",
    "cart_id",
    "source"
  ],
  "title": "Sharecart Merge Failed",
  "type": "object"
}