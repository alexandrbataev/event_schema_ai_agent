{
  "description": "Каждый раз, когда пользователь попадает в seo-каталог",
  "properties": {
    "has_delivery_address": {
      "description": "Флаг наличия введенного адреса на момент захода пользователем в SEO: если есть введенный адрес - 1, иначе 0",
      "type": "boolean"
    },
    "vertical": {
      "description": "Вертикаль, в которую попал пользователь (нужно будет добавить enum)",
      "type": "string"
    },
    "source": {
      "description": "Источник добавления товара",
      "type": "string",
      "enum": [
        "category_multiretail_seo"
      ]
    }
  },
  "required": [
    "source",
    "vertical",
    "has_delivery_address"
  ],
  "title": "SEO Landing Viewed",
  "type": "object"
}
  