{
  "description": "Взаимодействие со шторкой-предупреждением о выходе из SPG",
  "properties": {
    "trigger": {
      "type": "string",
      "description": "После какого экрана появилось предупреждение (cart, error, background)"
    },
    "source": {
      "type": "string",
      "description": "После нажатия на какую кнопку появилось предупреждение",
      "enum": [
        "arrow_buttom",
        "back_buttom",
        "background"
      ]
    },
    "exit": {
      "type": "string",
      "description": "Как ушёл от шторки",
      "enum": [
        "stay_buttom",
        "leave_buttom",
        "x",
        "swipe",
        "back_buttom",
        "background",
        "overlay"
      ]
    }
  },
  "required": [
    "trigger",
    "source",
    "exit"
  ],
  "title": "Spg Exit Warning",
  "type": "object"
}