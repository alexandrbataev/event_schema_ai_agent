{
  "description": "Клик на кнопку скролла в категорийной полке в каталоге",
  "properties": {
    "item_type": {
      "description": "тип сущности в полке",
      "type": "string",
      "enum": [
        "items"
      ]
    },
    "shelf_type": {
      "description": "Тип полки",
      "type": "string",
      "enum": [
        "category"
      ]
    },
    "category_id": {
      "description": "id категории полки",
      "type": "string"
    },
    "direction": {
      "description": "направление скролла: next - вправо, prev - влево",
      "type": "string",
      "enum": [
        "next",
        "prev"
      ]
    },
    "source": {
      "description": "Где произошло событие: 'main_page' – полка была на главной ритейлера, 'category' – полка находилась внутри какой-то категории",
      "type": "string",
      "enum": [
        "main_page",
        "category"
      ]
    },
    "shelf_position": {
      "description": "Позиция полки на странице, начиная с 1",
      "type": "number"
    },
    "parent_category_id": {
      "description": "id родительской категории (если полка внутри какой-то категории, если на главной ритейлера, то null)",
      "type": "string"
    },
    "parent_category_depth": {
      "description": "Уровень глубины родительской категории (parent_category_id)",
      "type": "number"
    }
  },
  "required": [
    "category_id",
    "source",
    "item_type",
    "shelf_type",
    "direction",
    "shelf_position",
    "parent_category_id",
    "parent_category_depth"
  ],
  "title": "Scroll Button Clicked",
  "type": "object"
}
