{
  "description": "Показана шторка Перед оформлением за",
  "properties": {
    "exit": {
      "type": "string",
      "description": "Как ушёл от шторки",
      "enum": [
        "continue scan",
        "checkout order",
        "x",
        "overlay",
        "back_button",
        "swipe"
      ]
    }
  },
  "required": [
    "exit"
  ],
  "title": "Spg Before Compliting Warning Closed",
  "type": "object"
}